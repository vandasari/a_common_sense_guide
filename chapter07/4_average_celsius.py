"""Algorithm: page 100Time complexity: O(n)There are two loops:    - the first converts the readings to celsius    - the second sums all the celsius numbers    Since there are two loops that each iterate over all n elements, we have n + n = 2n steps."""def average_celsius(fahrenheit):    # Collect celsius numbers:    celsius = []        # Convert each reading to celsius and add to array:    for degree in fahrenheit:        celsius_conversion = (degree - 32) / 1.8        celsius.append(celsius_conversion)            # Get sum of all celsius numbers    _sum = 0.0        for temperature in celsius:        _sum += temperature            return _sum / len(celsius)if __name__ == '__main__':    a = [32, 212]        res = average_celsius(a)        print(res)